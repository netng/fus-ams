<h2 class="fus-form-header mb-8">
  <%= t("custom.label.default_new_form", model: t("activerecord.models.role")).capitalize %>
</h2>

<%= render "form", role: @role %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.role")).capitalize) %>