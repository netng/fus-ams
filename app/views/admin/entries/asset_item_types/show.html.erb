<%= render "layouts/turbo_modal" do %>
  <h2 class="fus-form-header mb-2 sticky top-0 p-3 z-10 bg-fus-blue text-white shadow-md ">
    <%= t("custom.label.show_detail", model: t("activerecord.models.asset_item_type")).capitalize %>
  </h2>
	<div data-action="turbo:submit-end->modal#submitEnd" class="p-8">
    <div class="bg-slate-50 rounded-md">
      <div class="grid grid-cols-2 gap-2 p-4">
        <div class="mb-2">
          <label class="fus-label">
            <%= t("activerecord.attributes.asset_item_type.id_asset_item_type") %>
          </label>
          <span class="fus-readonly"><%= @asset_item_type.id_asset_item_type %></span>
        </div>

        <div class="mb-2">
          <label class="fus-label">
            <%= t("activerecord.attributes.asset_item_type.name") %>
          </label>
          <span class="fus-readonly"><%= @asset_item_type.name %></span>
        </div>

        <div class="mb-2">
          <label class="fus-label">
            <%= t("activerecord.models.asset_type") %>
          </label>
          <span class="fus-readonly"><%= @asset_item_type.asset_type.name %></span>
        </div>

      </div>

      <div class="mb-2 p-4">
        <label class="fus-label">
          <%= t("activerecord.attributes.asset_item_type.description") %>
        </label>
        <span class="fus-readonly"><%= @asset_item_type.description.blank? ? "-" : @asset_item_type.description %></span>
      </div>
      
    </div>

    <%= button_tag t("custom.label.cancel"),
      data: { action: "modal#hideModal" },
      class: "fus-secondary-btn md:w-[100px] mt-2"
    %>
    
	</div>
<% end %>


<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.asset_item_type")).capitalize) %>