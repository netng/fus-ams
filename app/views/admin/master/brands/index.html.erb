<h2 class="fus-form-header mb-4"><%= t("custom.label.asset_index_header", model: t("activerecord.models.brand")).capitalize %></h2>

<%= render "action_group" %>
<div class="table-brand" data-controller="table">
  <%= render "table" %>
  <% if policy([:admin, :authorization]).destroy? %>
    <%= render "destroy_button" %>
  <% end %>
</div>

<% content_for(:title, t("custom.label.asset_index_header", model: t("activerecord.models.brand")).capitalize) %>