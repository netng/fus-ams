<h2 class="fus-form-header mb-8">
  <%= t("custom.label.default_new_form", model: t("activerecord.models.site_default")).capitalize %>
</h2>

<%= render "form", site_default: @site_default %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.site_default")).capitalize) %>