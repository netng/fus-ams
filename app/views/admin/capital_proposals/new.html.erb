<h2 class="text-2xl font-bold text-gray-600 mb-8">
  <%= t("custom.label.default_new_form", model: t("activerecord.models.capital_proposal")).capitalize %>
</h2>

<%= render "form", capital_proposal: @capital_proposal %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.capital_proposal")).capitalize) %>