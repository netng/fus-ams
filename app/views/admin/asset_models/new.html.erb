<h2 class="fus-form-header mb-8">
  <%= t("custom.label.default_new_form", model: t("activerecord.models.asset_model")).capitalize %>
</h2>

<%= render "form", asset_model: @asset_model %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.asset_model")).capitalize) %>