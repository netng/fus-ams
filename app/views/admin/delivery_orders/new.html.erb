<%= render "layouts/turbo_modal" do %>
  <h2 class="fus-form-header mb-2 sticky top-0 p-3 z-10 bg-blue-400 text-white shadow-md ">
    <%= t("custom.label.new_form", model: t("activerecord.models.delivery_order")).capitalize %>
  </h2>
	<div data-action="turbo:submit-end->modal#submitEnd" class="p-8">
        <%= render "form", delivery_order: @delivery_order %>
	</div>
<% end %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.delivery_order")).capitalize) %>