<h2 class="text-2xl font-bold text-gray-600 mb-8"><%= t("custom.label.new_form", model: t("activerecord.models.brand")).capitalize %></h2>
<%= render "form", brand: @brand %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.brand")).capitalize) %>