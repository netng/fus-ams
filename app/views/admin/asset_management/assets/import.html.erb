<h2 class="fus-form-header mb-4">
  <%= t("custom.label.import_form", model: t("custom.label.data_asset")).capitalize %>
</h2>

<%= render "import_asset_registrations" %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.asset")).capitalize) %>
