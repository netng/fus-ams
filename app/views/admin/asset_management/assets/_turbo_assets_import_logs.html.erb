<div class="px-4 py-2 text-white">
  <% if error_message.blank? %>
    <% if state == "preparing_file" %>
      <span class="font-semibold">🛑 Please wait. Preparing files for import. It may take some time if the dataset is large.</span>
    <% elsif state == "preparing" %>
      <span class="font-semibold">[ 🌱 PREPARING ]</span> - Row: <%= row_index %> - Tagging id: <%= tagging_id %>
    <% elsif state == "importing" %>
      <span class="font-semibold">[ 🚀 IMPORTING ]</span> - Importing the data to database with batch. Please wait...
    <% else %>
      <span class="font-semibold text-green-300">[ 🎉 SUCCESS ]</span> - Import successfully.
    <% end %>
  <% else %>
    <span class="text-red-500">
      ❌ Import failed with error:
      <span class="font-bold"><%= error_message %></span>.
      Rolling back data.
    </span>
  <% end %>
</div>