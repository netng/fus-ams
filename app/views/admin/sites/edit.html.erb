<h2 class="text-2xl font-bold text-gray-600 mb-8">
  <%= t("custom.label.edit_form", model: t("activerecord.models.site")).capitalize %>
</h2>

<%= render "form", site: @site %>

<% content_for(:title, t("custom.label.edit_form", model: t("activerecord.models.site")).capitalize) %>