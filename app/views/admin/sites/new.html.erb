<h2 class="fus-form-header mb-8">
  <%= t("custom.label.default_new_form", model: t("activerecord.models.site")).capitalize %>
</h2>

<%= render "form", site: @site %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.site")).capitalize) %>