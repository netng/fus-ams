<%= render "layouts/turbo_modal" do %>
  <h2 class="fus-form-header mb-8">
    <%= t("custom.label.new_form", model: t("activerecord.models.user_asset")).capitalize %>
  </h2>
	<div data-action="turbo:submit-end->modal#submitEnd">
        <%= render "form", user_asset: @user_asset %>
	</div>
<% end %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.user_asset")).capitalize) %>