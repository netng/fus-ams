<%= render "layouts/turbo_modal" do %>
  <h2 class="fus-form-header mb-8">
    <%= t("custom.label.default_new_form", model: t("activerecord.models.site_stat")).capitalize %>
  </h2>
	<div data-action="turbo:submit-end->modal#submitEnd">
        <%= render "form", site_stat: @site_stat %>
	</div>
<% end %>

<% content_for(:title, t("custom.label.new_form", model: t("activerecord.models.site_stat")).capitalize) %>