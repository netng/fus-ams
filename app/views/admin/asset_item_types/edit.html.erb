<h2 class="fus-form-header mb-8">
  <%= t("custom.label.edit_form", model: t("activerecord.models.asset_item_type")).capitalize %>
</h2>

<%= render "form", asset_item_type: @asset_item_type %>

<% content_for(:title, t("custom.label.edit_form", model: t("activerecord.models.asset_item_type")).capitalize) %>