<h2 class="fus-form-header mb-8">
  <%= t("custom.label.edit_form", model: t("activerecord.models.capital_proposal")).capitalize %>
</h2>

<%= render "form", capital_proposal: @capital_proposal %>

<% content_for(:title, t("custom.label.edit_form", model: t("activerecord.models.capital_proposal")).capitalize) %>