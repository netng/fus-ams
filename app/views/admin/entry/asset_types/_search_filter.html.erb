<div class="bg-slate-50 rounded-lg mt-4 mb-4 p-4 hidden" data-toggle-target="toggle">
  <%= search_form_for [:admin, @q], url: admin_asset_types_path, data: { 
    controller: "filters",
    turbo_frame: :table,
    turbo_action: "advance",
    filters_delay_value: 200,
    filters_table_outlet: ".table-asset_type"
  } do |f| %>

    <div class="flex flex-wrap md:flex md:flex-nowrap gap-2">
      <div class="w-full mb-2">
        <%= f.label "#{t('activerecord.attributes.asset_type.id_asset_type')}", class: "fus-label" %>
        <%= f.search_field :id_asset_type_cont, data: { action: "filters#submit" }, class: "fus-input" %>
      </div>

      <div class="w-full mb-2">
        <%= f.label "#{t('activerecord.attributes.asset_type.name')}", class: "fus-label" %>
        <%= f.search_field :name_cont, data: { action: "filters#submit" }, class: "fus-input" %>
      </div>
    </div>
    
  <% end %>
</div>