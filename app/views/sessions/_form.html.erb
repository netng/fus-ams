<%= form_with url: auth_sign_in_path, local: false do |form| %>
  <div class="mb-2">
    <%= label_tag :username, "#{t('custom.label.username')}", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= text_field_tag :username, params[:username], class: "block w-full p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500", required: true %>
  </div>

  <div class="mb-2">
    <%= label_tag :password, "#{t('custom.label.password')}", class: "block text-sm font-medium text-gray-700 mb-2" %>
    <%= password_field_tag :password, params[:password], class: "block w-full p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500", required: true %>
  </div>

  <div class="mb-2">
    <%= submit_tag "#{t('custom.label.sign_in_anchor')}", class: "w-full bg-fus-blue hover:cursor-pointer text-white p-3 rounded-lg hover:bg-blue-500 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
  </div>
<% end %>